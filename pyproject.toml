[project]
name = "agent-memory-failure-demo"
version = "0.1.0"
description = "When Summaries Fail: A minimal demonstration of information loss in agent memory"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
]

[project.scripts]
demo = "run:main"
